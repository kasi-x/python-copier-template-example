project:
  execute-dir: project

bibliography: references/references.bib
csl: references/chicago-author-date.csl

# quartodoc generates API reference pages from python_copier_template_example docstrings.
# Run `quarto add machow/quartodoc` once, then `quartodoc build --config _quarto.yml`.
quartodoc:
  package: python_copier_template_example
  dir: docs/reference
  title: API Reference
  renderer:
    style: pkg
