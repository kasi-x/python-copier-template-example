{"config":{"lang":["en"],"separator":"[\\s\\-_,:!=\\[\\]()\\\\\"`/]+|\\.(?!\\d)"},"items":[{"location":"","level":1,"title":"python-copier-template-example","text":"<p>An expanded https://github.com/kasi-x/python-copier-template to illustrate how it looks with all the options enabled.</p> <p>This is where you should write a short paragraph that describes what your module does, how it does it, and why people should use it.</p>","path":["Home"],"tags":[]},{"location":"#installation","level":2,"title":"Installation","text":"<pre><code>$ pip install kasi-x-python-copier-template-example\n</code></pre>","path":["Home"],"tags":[]},{"location":"#usage","level":2,"title":"Usage","text":"<pre><code>from python_copier_template_example import __version__\n\nprint(f\"Hello python_copier_template_example {__version__}\")\n</code></pre>","path":["Home"],"tags":[]},{"location":"modules/","level":1,"title":"Modules","text":"<p>Top level API.</p> <p>.. data:: version     :type: str</p> <pre><code>Version number as calculated by https://github.com/pypa/setuptools_scm\n</code></pre>","path":["Modules"],"tags":[]},{"location":"how-to/run-container/","level":1,"title":"Run in a container","text":"<p>Pre-built containers with python-copier-template-example and its dependencies already installed are available on Github Container Registry.</p>","path":["Run in a container"],"tags":[]},{"location":"how-to/run-container/#starting-the-container","level":2,"title":"Starting the container","text":"<p>Pre-built images are published to the container registry on every release. To pull the container from github container registry and run:</p> <pre><code>$ docker run ghcr.io/kasi-x/python-copier-template-example:latest --version\n</code></pre> <p>To get a released version, use a numbered release instead of <code>latest</code>.</p>","path":["Run in a container"],"tags":[]},{"location":"how-to/run-container/#logging-in-the-cloud","level":2,"title":"Logging in the cloud","text":"<p>Logs go to stderr as human-readable console text by default; set the <code>LOG_FORMAT=json</code> environment variable to switch to one-JSON-object-per-line instead, which log aggregators (CloudWatch Logs, Cloud Logging, Azure Monitor, journald, ...) can parse. This is independent of which (if any) cloud SDK dependencies were added, and of which platform runs the container — the same image works locally (console) and in any cloud (JSON), so there's nothing extra to build per target: just set the environment variable wherever the container runs.</p> <p>Render, Railway and Vercel all colour-code and filter by severity in their log viewer if the JSON has a <code>level</code> + <code>message</code> field pair; <code>logging_setup.py</code>'s JSON mode already uses <code>level</code> but names the message field <code>event</code> (structlog's own convention), so those three won't pick up severity colouring out of the box. If that matters to you, swap <code>\"event\"</code> for <code>\"message\"</code> in <code>logging_setup.py</code> — logs are fully readable either way, this only affects that colouring/filtering.</p>","path":["Run in a container"],"tags":[]},{"location":"tutorials/installation/","level":1,"title":"Installation","text":"","path":["Installation"],"tags":[]},{"location":"tutorials/installation/#check-your-version-of-python","level":2,"title":"Check your version of python","text":"<p>You will need python 3.11 or later. You can check your version of python by typing into a terminal:</p> <pre><code>$ python3 --version\n</code></pre>","path":["Installation"],"tags":[]},{"location":"tutorials/installation/#create-a-virtual-environment","level":2,"title":"Create a virtual environment","text":"<p>It is recommended that you install into a “virtual environment” so this installation will not interfere with any existing Python software:</p> <pre><code>$ python3 -m venv /path/to/venv\n$ source /path/to/venv/bin/activate\n</code></pre>","path":["Installation"],"tags":[]},{"location":"tutorials/installation/#installing-the-library","level":2,"title":"Installing the library","text":"<p>You can now use <code>pip</code> to install the library and its dependencies:</p> <pre><code>$ python3 -m pip install kasi-x-python-copier-template-example\n</code></pre> <p>If you require a feature that is not currently released you can also install from github:</p> <pre><code>$ python3 -m pip install git+https://github.com/kasi-x/python-copier-template-example.git\n</code></pre> <p>The library should now be installed and the commandline interface on your path. You can check the version that has been installed by typing:</p> <pre><code>$ python-copier-template-example --version\n</code></pre>","path":["Installation"],"tags":[]}]}